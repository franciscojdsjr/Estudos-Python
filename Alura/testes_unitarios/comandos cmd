 criar relatorio html || pytest --cov=bytebank/ tests/ --cov-report html
  testa a cobertura dos testes  || pytest --cov || especicando pasta pytest --cov=bytebank/codigo.py tests/
 avalia quais linhas estão sem testes || pytest --cov=bytebank/ tests/ --cov-report term-missing

ativar ambiente virtual || source "/home/francisco/Devlopment/Python Scripts/Alura/testes_unitarios/venv/bin/activate"
gerar requiriments || pip freeze > requirements.txt
git push origin master

pytest veroboso -- pytest -v

crirar report xml -- pytest --junitxml report.xml

criar ambiente virtual -- source venv/bin/activate